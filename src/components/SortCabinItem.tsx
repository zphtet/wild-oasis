import React from "react";

type SortItemProps = {
  name: string;
  field: string;
};

const SortCabinItem = ({ name, field }: SortItemProps) => {
  return <option>nam</option>;
};

export default SortCabinItem;
